# Copy this file to .env and fill in the values
JWT_SECRET=replace-with-a-long-random-secret
# DB_PATH=data/chadstart.db  # Optional: path to SQLite database file (default: chadstart.db)
# CHADSTART_HANDLERS_FOLDER=handlers  # Optional: folder for custom endpoint/middleware handler files (default: handlers)
